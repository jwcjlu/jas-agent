# Elasticsearch 通配符智能匹配功能说明

## 🎯 功能概述

当 `search_indices` 工具发现多个索引具有**相同前缀**，仅**日期不同**时，会自动建议使用**通配符 `*`** 来一次性查询所有相关索引。

---

## ✨ 核心功能

### 1. 自动检测相同前缀索引

**算法逻辑** (`tools/es_index_search.go`):
```go
// 检测步骤
1. 找到所有匹配的索引
2. 提取公共前缀
3. 检查后缀是否像日期（包含数字和分隔符 -._）
4. 如果匹配，生成通配符建议

// 示例
输入索引:
  - backend-vm_manager-2025.11.04
  - backend-vm_manager-2025.11.03
  - backend-vm_manager-2025.11.02

检测:
  公共前缀: backend-vm_manager
  后缀: 2025.11.04, 2025.11.03, 2025.11.02
  判断: 都是日期格式 ✅

建议: backend-vm_manager-*
```

### 2. 智能提示

**search_indices 返回示例**:
```
找到 3 个包含关键词 'backend' 的索引：

- backend-vm_manager-2025.11.04
  Health: green, Docs: 15234, Size: 2.3mb
- backend-vm_manager-2025.11.03
  Health: green, Docs: 14567, Size: 2.1mb
- backend-vm_manager-2025.11.02
  Health: green, Docs: 13890, Size: 2.0mb

💡 建议：这些索引具有相同前缀，可以使用通配符 'backend-vm_manager-*' 一次性查询所有相关索引。
```

### 3. Agent 自动应用

**系统提示词已包含**:
```
通配符使用策略:
  ⭐ 当发现多个索引具有相同前缀，仅日期不同时，使用通配符 *
  
  示例: search_indices 返回多个同前缀索引
  Thought: 发现具有相同前缀，应该使用通配符
  Action: search_documents[{"index": "backend-vm_manager-*", ...}]
```

**Few-shot 示例**:
```
Observation: search_indices返回：
  backend-vm_manager-2025.11.04
  backend-vm_manager-2025.11.03
  backend-vm_manager-2025.11.02

Thought: 发现3个索引具有相同前缀backend-vm_manager-，仅日期不同
        应该使用通配符backend-vm_manager-*来查询所有数据

Action: search_documents[{
  "index": "backend-vm_manager-*",    ⭐ 使用通配符
  "query": {"term": {"L.keyword": "ERROR"}}
}]
```

---

## 📊 实际使用效果

### 场景 1: 查询多天的错误日志

**用户**: "查询backend项目最近3天的错误日志"

**Agent 执行**:
```
💭 Thought: 用户提到backend，先查找相关索引
⚙️ Action: search_indices[backend]
👁️ Observation:
  找到 3 个包含 'backend' 的索引：
  - backend-vm_manager-2025.11.04
  - backend-vm_manager-2025.11.03
  - backend-vm_manager-2025.11.02
  
  💡 建议：可以使用通配符 'backend-vm_manager-*'    ⭐ 工具自动建议

💭 Thought: search_indices建议使用通配符，这样可以一次查询所有天的数据
⚙️ Action: search_documents[{
  "index": "backend-vm_manager-*",                     ⭐ 使用通配符
  "query": {
    "bool": {
      "must": [
        {"term": {"L.keyword": "ERROR"}},
        {"range": {"T": {"gte": "now-3d"}}}
      ]
    }
  }
}]
👁️ Observation: 从3个索引中找到 45 条错误日志

📊 最终答案: 在 backend-vm_manager 项目最近3天的日志中找到 45 条错误...
```

### 场景 2: 趋势分析（跨多个索引）

**用户**: "分析11月份的错误趋势"

**Agent 执行**:
```
💭 Thought: 用户想分析11月的数据，先查找11月的索引
⚙️ Action: search_indices[2024-11]
👁️ Observation:
  找到 30 个包含 '2024-11' 的索引：
  - logs-2024-11-01
  - logs-2024-11-02
  - logs-2024-11-03
  ...
  - logs-2024-11-30
  
  💡 建议：可以使用通配符 'logs-2024-11-*'         ⭐ 工具建议

💭 Thought: 有30个索引，使用通配符可以一次性查询整个11月
⚙️ Action: aggregate_data[{
  "index": "logs-2024-11-*",                           ⭐ 通配符匹配30个索引
  "query": {"term": {"L.keyword": "ERROR"}},
  "aggs": {
    "daily_trend": {
      "date_histogram": {
        "field": "T",
        "calendar_interval": "day"
      }
    }
  }
}]
👁️ Observation: 聚合结果（11月每天的错误统计）

📊 最终答案: 11月份错误日志趋势：
  11-01: 120条
  11-02: 98条
  11-03: 145条
  ...
  峰值在 11-15 (234条)
```

### 场景 3: 多项目对比

**用户**: "对比frontend和backend的错误率"

**Agent 执行**:
```
💭 Thought: 需要分别查找frontend和backend的索引
⚙️ Action: search_indices[frontend]
👁️ Observation:
  - frontend-app-2025.11.04
  - frontend-app-2025.11.03
  💡 建议：使用 'frontend-app-*'

💭 Thought: 使用通配符查询frontend所有错误
⚙️ Action: search_documents[{
  "index": "frontend-app-*",                           ⭐ 通配符
  "query": {"term": {"L.keyword": "ERROR"}},
  "size": 0
}]
👁️ Observation: 总共 89 条错误

💭 Thought: 现在查找backend的索引
⚙️ Action: search_indices[backend]
👁️ Observation:
  - backend-vm_manager-2025.11.04
  - backend-vm_manager-2025.11.03
  💡 建议：使用 'backend-vm_manager-*'

💭 Thought: 使用通配符查询backend所有错误
⚙️ Action: search_documents[{
  "index": "backend-vm_manager-*",                     ⭐ 通配符
  "query": {"term": {"L.keyword": "ERROR"}},
  "size": 0
}]
👁️ Observation: 总共 45 条错误

📊 最终答案: 
  Frontend 错误: 89条
  Backend 错误: 45条
  Frontend 错误率更高，需要重点关注
```

---

## 🔧 技术实现

### 公共前缀检测算法

```go
func findCommonPrefix(strs []string) string {
    // 例如：
    // ["backend-vm_manager-2025.11.04",
    //  "backend-vm_manager-2025.11.03",
    //  "backend-vm_manager-2025.11.02"]
    
    prefix := strs[0]  // backend-vm_manager-2025.11.04
    
    for each str in strs[1:] {
        // 逐字符比较
        for i := 0; i < len(prefix) && i < len(str); i++ {
            if prefix[i] != str[i] {
                prefix = prefix[:i]  // 截断到不同的位置
                break
            }
        }
    }
    
    // prefix = "backend-vm_manager-2025.11.0"
    // 去掉末尾的分隔符
    prefix = trimRight(prefix, "-_.")
    
    // 返回: "backend-vm_manager"
}
```

### 日期后缀检测

```go
func isDateLikeSuffix(suffix string) bool {
    // 检查后缀是否像日期
    // 例如: "2025.11.04", "2024-11-01", "20241104"
    
    hasDigit := 是否包含数字
    hasDateSeparator := 是否包含 -_.
    
    return hasDigit && hasDateSeparator
}
```

---

## 📋 支持的索引命名模式

### 模式 1: 项目-日期

```
索引列表:
  - backend-2025.11.04
  - backend-2025.11.03
  - backend-2025.11.02

通配符: backend-*
```

### 模式 2: 项目-功能-日期

```
索引列表:
  - backend-vm_manager-2025.11.04
  - backend-vm_manager-2025.11.03
  - backend-vm_manager-2025.11.02

通配符: backend-vm_manager-*
```

### 模式 3: 功能-年月-日

```
索引列表:
  - logs-2024-11-01
  - logs-2024-11-02
  - logs-2024-11-03

通配符: logs-2024-11-*  （11月所有日志）
或      logs-*          （所有日志）
```

### 模式 4: 带点分隔的日期

```
索引列表:
  - api.logs.2024.11.04
  - api.logs.2024.11.03
  - api.logs.2024.11.02

通配符: api.logs.*
```

---

## 🎯 通配符的优势

### 1. 简化查询

**不使用通配符**:
```json
{
  "index": "logs-2024-11-01,logs-2024-11-02,logs-2024-11-03,...",
  "query": {...}
}
```
❌ 复杂、容易出错、有长度限制

**使用通配符**:
```json
{
  "index": "logs-2024-11-*",
  "query": {...}
}
```
✅ 简洁、清晰、无长度限制

### 2. 避免遗漏数据

```
查询多天数据:
  不用通配符 → 可能漏掉某天的索引
  用通配符   → 自动包含所有匹配的索引 ✅
```

### 3. 适合趋势分析

```
统计一个月的错误趋势:
  index: "logs-2024-11-*"        ⭐ 一次查询30天
  aggs: date_histogram by day
  
结果: 完整的月度趋势图
```

### 4. 自动适应新索引

```
今天创建新索引: logs-2024-11-05
使用通配符: logs-2024-11-*
结果: 自动包含新索引，无需修改查询 ✅
```

---

## 📊 完整的工作流程

```
用户: "查询backend项目的错误日志"
  ↓
Agent: search_indices[backend]
  ↓
工具返回:
  - backend-vm_manager-2025.11.04
  - backend-vm_manager-2025.11.03
  - backend-vm_manager-2025.11.02
  💡 建议：使用 'backend-vm_manager-*'    ⭐ 自动建议
  ↓
Agent 理解建议:
  Thought: 发现相同前缀，应该使用通配符
  ↓
Agent 应用通配符:
  Action: search_documents[{
    "index": "backend-vm_manager-*",        ⭐ 使用通配符
    "query": {"term": {"L.keyword": "ERROR"}}
  }]
  ↓
ES 自动查询所有匹配的索引
  ↓
返回: 所有3个索引的聚合结果
```

---

## 🔍 检测算法示例

### 示例 1: 检测成功

**输入**:
```
backend-vm_manager-2025.11.04
backend-vm_manager-2025.11.03
backend-vm_manager-2025.11.02
```

**处理**:
```
公共前缀: "backend-vm_manager-2025.11.0"
去除分隔符: "backend-vm_manager"
后缀检查: "2025.11.04" ✅ 是日期
建议: "backend-vm_manager-*"
```

### 示例 2: 检测成功（横线分隔）

**输入**:
```
logs-2024-11-01
logs-2024-11-02
logs-2024-11-03
```

**处理**:
```
公共前缀: "logs-2024-11-0"
去除分隔符: "logs-2024-11"
后缀检查: "01", "02", "03" ✅ 是日期
建议: "logs-2024-11-*"
```

### 示例 3: 不检测（前缀不同）

**输入**:
```
backend-logs
frontend-logs
api-logs
```

**处理**:
```
公共前缀: "" （无公共前缀）
建议: ""（不建议通配符）
```

### 示例 4: 不检测（后缀非日期）

**输入**:
```
users-active
users-inactive
users-pending
```

**处理**:
```
公共前缀: "users-"
后缀检查: "active", "inactive" ❌ 不是日期格式
建议: ""（不建议通配符）
```

---

## 📝 系统提示词更新

### 新增策略说明

```
通配符使用策略（重要！）:
  ⭐ 当发现多个索引具有相同前缀，仅日期不同时，使用 *
  
  好处:
    ✅ 一次查询多个时间段
    ✅ 避免遗漏数据
    ✅ 简化查询语句
    ✅ 适合趋势分析
```

### 新增示例（第6个）

```
示例 6: 使用通配符匹配多索引

Observation: search_indices返回多个同前缀索引
Thought: 仅日期不同，使用通配符
Action: search_documents[{"index": "前缀-*", ...}]
```

---

## 🎯 应用场景

### 1. 历史数据查询

```
查询: "统计本月所有的错误"
索引: logs-2024-11-01 ~ logs-2024-11-30
使用: logs-2024-11-*                    ⭐ 一次查询30天
```

### 2. 跨日志聚合

```
查询: "对比前后端的错误率"
Backend: backend-vm_manager-*           ⭐ 所有backend日志
Frontend: frontend-app-*                ⭐ 所有frontend日志
```

### 3. 时间序列分析

```
查询: "展示过去一周的错误趋势"
索引: logs-2024-11-*                    ⭐ 覆盖整个月
过滤: range query 限定时间为过去7天
聚合: date_histogram by day
```

---

## ✅ 优化效果对比

### 优化前 ❌

```
Agent: search_indices[backend]
返回:
  - backend-vm_manager-2025.11.04
  - backend-vm_manager-2025.11.03
  - backend-vm_manager-2025.11.02

Agent: 逐个查询或手动拼接索引名
Action: search_documents[{
  "index": "backend-vm_manager-2025.11.04,backend-vm_manager-2025.11.03,backend-vm_manager-2025.11.02",
  ...
}]

问题:
  ❌ 索引名太长
  ❌ 容易出错
  ❌ 有长度限制
```

### 优化后 ✅

```
Agent: search_indices[backend]
返回:
  - backend-vm_manager-2025.11.04
  - backend-vm_manager-2025.11.03
  - backend-vm_manager-2025.11.02
  💡 建议：使用 'backend-vm_manager-*'              ⭐ 自动建议

Agent: 理解并应用建议
Action: search_documents[{
  "index": "backend-vm_manager-*",                   ⭐ 简洁优雅
  ...
}]

优势:
  ✅ 简洁清晰
  ✅ 自动包含所有匹配
  ✅ 适应新增索引
```

---

## 🎊 总结

**已实现**:
1. ✅ **自动检测** - 识别相同前缀+日期后缀的索引
2. ✅ **智能建议** - search_indices 自动提供通配符建议
3. ✅ **提示词教学** - 系统提示词包含通配符使用策略
4. ✅ **Few-shot 示例** - 第6个示例展示通配符用法

**Agent 现在会**:
- 🎯 自动识别同前缀索引
- 🎯 优先使用通配符简化查询
- 🎯 一次查询多个时间段
- 🎯 提供更准确的趋势分析

**编译验证**: ✅ 通过

**立即体验智能通配符匹配功能！** 🚀
