# JAS Agent React 前端 - 快速启动指南

## 问题已修复 ✅

所有必要的文件已经就绪，现在您可以正常运行 React 前端了！

## 🚀 启动步骤

### 1. 确认依赖已安装

```bash
cd web
npm install
```

如果已经安装过，可以跳过这一步。

### 2. 启动后端服务器

在另一个终端窗口：

```bash
cd cmd/server
go run main.go -apiKey YOUR_API_KEY -baseUrl YOUR_BASE_URL
```

### 3. 启动 React 开发服务器

```bash
cd web
npm run dev
```

### 4. 访问应用

打开浏览器访问：http://localhost:3000

## 📋 可用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器（端口 3000） |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产构建 |
| `npm run lint` | 运行 ESLint 检查 |

## 🔍 常见问题

### Q: 端口被占用？

如果 3000 端口被占用，可以修改 `vite.config.js` 中的端口：

```javascript
server: {
  port: 3001,  // 改为其他端口
  ...
}
```

### Q: API 请求失败？

1. 确认后端服务器已启动（端口 8080）
2. 检查浏览器控制台的 Network 标签
3. 确认 vite.config.js 中的代理配置正确

### Q: WebSocket 连接失败？

确保：
1. 后端支持 WebSocket
2. 防火墙没有阻止连接
3. 检查浏览器控制台错误信息

## 📂 项目结构

```
web/
├── src/
│   ├── components/      # React 组件
│   ├── services/        # API 服务
│   ├── App.jsx          # 主应用
│   ├── main.jsx         # 入口
│   └── index.css        # 全局样式
├── index.html           # HTML 模板
├── vite.config.js       # Vite 配置
├── package.json         # 依赖配置
└── .eslintrc.cjs        # ESLint 配置
```

## ✅ 修复了什么

1. ✅ 更新了 `index.html` 为 React 版本
2. ✅ 添加了 `.gitignore` 文件
3. ✅ 添加了 `.eslintrc.cjs` 配置
4. ✅ 修正了 Vite 构建输出目录

## 🎉 现在可以开始使用了！

运行 `npm run dev` 后，您应该能看到：

```
VITE v5.0.0  ready in xxx ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
➜  press h to show help
```

然后访问 http://localhost:3000 即可使用 React 前端界面！

## 📚 更多信息

查看完整文档：[README.md](./README.md)


